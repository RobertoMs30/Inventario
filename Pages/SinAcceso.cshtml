@page
@{
    Layout = "_Layout";
    ViewData["Title"] = "Sin acceso";
}

<div class="text-center mt-5">
    <div style="font-size:4rem">⛔</div>
    <h2 class="mt-3">Sin acceso</h2>
    <p class="text-muted">No tienes permiso para ver esta página.<br/>Contacta al administrador si crees que es un error.</p>
    <a href="/" class="btn btn-secondary mt-2">Ir al inicio</a>
</div>
